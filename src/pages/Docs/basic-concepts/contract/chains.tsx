import { useParams } from 'react-router-dom';
import CodeBlock from '../../../../components/CodeBlock';

export const BasicConceptsUseContractPage = () => {
    const { version } = useParams();
    return (
        <>
            <div className='title'><div className='tag'>/</div>Chains</div>
            <br />
            So, having got acquainted with the methods for creating logical chains using <div className='code-tag'>OperationOutcome</div>, let's look at a comprehensive implementation example.
            tasks for initializing a new user.
            <br />
            
        </>
    );
}